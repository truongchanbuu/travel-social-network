import 'package:flutter/material.dart';

class TourTimeDetailBottomSheet extends StatelessWidget {
  const TourTimeDetailBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
