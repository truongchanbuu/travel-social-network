import 'package:flutter/material.dart';

class TourDetailTicketPage extends StatelessWidget {
  final String ticketId;
  const TourDetailTicketPage({super.key, required this.ticketId});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        // appBar: _buildAppBar(),
        );
  }
}
